# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1          # ASGI server (includes httptools + uvloop)

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30                 # ORM
psycopg2-binary==2.9.9             # PostgreSQL driver
alembic==1.13.1                    # Database migrations (optional but recommended)

# ── AWS SDK ────────────────────────────────────────────────────────────────────
boto3==1.34.131                    # AWS SDK — used for Secrets Manager
botocore==1.34.131                 # boto3 dependency (pin for reproducibility)

# ── Data validation ────────────────────────────────────────────────────────────
pydantic==2.7.3                    # Pydantic v2 (FastAPI's built-in validation)
pydantic-settings==2.3.1           # Settings management via env vars

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1               # Load .env file in local dev
python-multipart==0.0.9            # Needed for form / file upload support
httpx==0.27.0                      # Async HTTP client (used in tests)

# ── Testing (optional — install with: pip install -r requirements-dev.txt) ─────
# pytest==8.2.2
# pytest-asyncio==0.23.7
# httpx==0.27.0